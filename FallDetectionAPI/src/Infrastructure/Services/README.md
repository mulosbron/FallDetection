# Infrastructure Services Note

This folder is reserved for email, notification, and external service integration services.
Currently, AI and Mock Camera adapters are located under `src/Infrastructure/Integration`.
