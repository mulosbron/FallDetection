meta {
  name: Results statistics ai-service
  type: http
  seq: 1
}

get {
  url: {{webApiBase}}/api/results/statistics/ai-service
  body: none
  auth: none
}

tests {
  test("status 200 or 503", function () {
    expect([200, 503]).to.include(res.getStatus());
  });
}
