meta {
  name: Results health ai-service
  type: http
  seq: 5
}

get {
  url: {{webApiBase}}/api/results/health/ai-service
  body: none
  auth: none
}

tests {
  test("status 200 or 503", function () {
    expect([200, 503]).to.include(res.getStatus());
  });
}
