meta {
  name: Frames raw upload camera1
  type: http
  seq: 1
}

post {
  url: {{webApiBase}}/api/frames/raw?cameraId=1
  body: text
  auth: none
}

headers {
  Content-Type: image/jpeg
}

body:text {
  bruno-test-jpeg-payload
}

tests {
  test("status 200 or 503", function () {
    expect([200, 503]).to.include(res.getStatus());
  });
}
