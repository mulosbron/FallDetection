meta {
  name: Notify fall-detected
  type: http
  seq: 1
}

post {
  url: {{signalrBase}}/internal/notify/fall-detected
  body: json
  auth: none
}

body:json {
  {
    "cameraId": "cam-001",
    "cameraName": "Kamera 1",
    "timestamp": "2026-01-01T00:00:00Z",
    "confidence": 0.95,
    "message": "bruno-e2e-fall-test",
    "yesCount": 1,
    "totalCount": 1
  }
}

tests {
  test("status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("published", function () {
    var body = res.getBody();
    expect(body.published).to.equal(true);
  });
}
